.vercel
.env
*.env
!*.env.example
__pycache__/
*.pyc
.devtmp/

# macOS
.DS_Store

# Generated by the seo skill — regenerated each audit, never hand-authoritative.
# Decisions and locks live in the vault note, not here:
#   06 Business/Steven John Hubbard LLC/MEND Medical/Copy & SEO/MEND SEO Audit - <date>.md
ACTION-PLAN.md
FULL-AUDIT-REPORT.md

# Vault-side, never part of the site: the folder index note and the full-res
# originals. Both are already in .vercelignore; ignoring them here is what stops
# a stray `git add -A` from sweeping hundreds of MB of PNGs into the deploy.
/index.md
/assets/
